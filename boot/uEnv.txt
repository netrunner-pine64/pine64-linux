console=tty0 console=ttyS0,115200n8 no_console_suspend
kernel_filename=kernel
root=/dev/mmcblk0p1
set_cmdline=setenv bootargs console=${console} ${optargs} earlycon=uart,mmio32,0x01c28000 mac_addr=${ethaddr} root=${root} rw rootwait boot=live components hostname=live-pc username=live earlyprintk=sunxi-uart,0x01c28000 loglevel=8
load_dtb=if test ${fdt_filename} = ""; then setenv fdt_filename ${fdt_filename_prefix}${pine64_model}${fdt_filename_suffix}; fi; load mmc ${boot_part} ${fdt_addr} boot/${fdt_filename}; fdt addr ${fdt_addr}; fdt resize
load_kernel=load mmc ${boot_part} ${kernel_addr} boot/${kernel_filename}
boot_kernel=booti ${kernel_addr} - ${fdt_addr}
